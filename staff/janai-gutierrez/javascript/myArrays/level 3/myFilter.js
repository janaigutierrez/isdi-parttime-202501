    // Filter - creates a copy of a portion of a given array filtered down

    const words = ['spray', 'elite', 'exuberant', 'destruction', 'present'];

    const result = words.filter((word) => word.lenght > 6);

    console.log(result); //expected output: ['exuberant, 'destruction', 'present'];

    function myFilter(wordlength) {

    }